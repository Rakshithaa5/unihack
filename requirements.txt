# ProvenIQ — Python dependencies
# All free-tier / open-source

# LLM / AI
groq>=0.9.0                       # Groq API: Llama 3.3 70B, Llama 3.2 Vision (free tier)

# PDF parsing
pdfplumber>=0.11.0
PyMuPDF>=1.24.0                    # fitz — layout-aware PDF

# HTML parsing
beautifulsoup4>=4.12.0
requests>=2.32.0
lxml>=5.2.0                        # fast HTML/XML parser for BS4

# Web search (free-tier REST wrapper)
ddgs>=7.0.0                          # zero-key web search (renamed from duckduckgo-search)

# RAG — embeddings & vector store
sentence-transformers>=3.0.0       # local, all-MiniLM-L6-v2, no API cost
chromadb>=0.5.0                    # local embedded vector store
torch>=2.3.0                       # required by sentence-transformers

# Agent orchestration
langgraph>=0.2.0                   # LangGraph for agentic pipelines
langchain-core>=0.2.0

# Backend
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9            # file upload support

# Storage
# SQLite is in the standard library — no extra package needed

# Utilities
pydantic>=2.7.0                    # data validation & schema
python-dotenv>=1.0.0               # .env support for API keys
httpx>=0.27.0                      # async HTTP for FastAPI
tqdm>=4.66.0                       # progress bars for batch runs
